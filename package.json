{
  "name": "oficin-ia-checklist-v15-22-tempo-real-microfone",
  "version": "15.22.1",
  "private": true,
  "description": "OFICIN-IA Checklist V15.22: PC e celular em tempo real, acompanhamento ao vivo, observações por voz e PDF de equipe.",
  "scripts": {
    "prepare:apk": "node scripts/prepare-apk.js",
    "android:icons": "node scripts/generate-android-icons.js",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "build:android": "npm run prepare:apk && if [ ! -d android ]; then npx cap add android; fi && npx cap sync android && npm run android:icons && cd android && chmod +x ./gradlew && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "6.2.1",
    "@capacitor/core": "6.2.1",
    "@capacitor-community/speech-recognition": "6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
